<UserControl x:Class="FilmsUCWpf.View.FilmGenreControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FilmsUCWpf.View"
             mc:Ignorable="d" 
             d:DesignHeight="20">
    <Grid Height="20">
        <Grid.Resources>
            <Style TargetType="TextBox" BasedOn="{StaticResource DarkTextBox}"/>
            <Style TargetType="Button" BasedOn="{StaticResource GrayButton}"/>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="GenreName" Text="{Binding Name}" Grid.Column="0"/>
        <CheckBox x:Name="CheckBox_IsSerial" IsChecked="{Binding IsSerialGenre}" Content="{DynamicResource st_is_serial}" 
                  HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="WhiteSmoke" Grid.Column="1"/>
        <Button x:Name="DeleteGenreButton" Content="{DynamicResource st_del_genre}" Width="100" HorizontalAlignment="Left" Click="DeleteGenreButton_Click" Grid.Column="2"/>
    </Grid>
</UserControl>
