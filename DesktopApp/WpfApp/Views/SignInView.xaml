<UserControl x:Class="WpfApp.Views.SignInView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel VerticalAlignment="Center">
            <TextBox Style="{StaticResource Wtextbox}" Height="20" Width="250"/>
            <Rectangle Height="25"/>
            <TextBox Style="{StaticResource Wtextbox}" Height="20" Width="250"/>
            <Rectangle Height="35"/>
            <Button Style="{StaticResource GrayButton}" Height="20" Width="100" Content="Sign In"/>
        </StackPanel>
        <Button Style="{StaticResource GrayButton}" Content="Return"
                VerticalAlignment="Bottom"/>
    </Grid>
</UserControl>
