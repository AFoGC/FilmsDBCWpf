<UserControl x:Class="WpfApp.Views.GenreConteinerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp.Views"
             mc:Ignorable="d">
    <Grid x:Name="grid">
        <Grid.Resources>
            <Style TargetType="TextBox" BasedOn="{StaticResource DarkTextBox}"/>
            <Style TargetType="Button" BasedOn="{StaticResource GrayButton}"/>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Label x:Name="MenuLabel" Content="Genres Settings" Foreground="White" BorderThickness="0 0 0 1" BorderBrush="#FF7A7A7A"/>
        <StackPanel x:Name="GenresPanel" Margin="0,5,0,0" Grid.Row="1"/>
        <Button x:Name="AddGenreButton" Content="Add new genre" Width="200" Height="20"
                HorizontalAlignment="Left" Click="AddGenreButton_Click" Grid.Row="2"/>


    </Grid>
</UserControl>
