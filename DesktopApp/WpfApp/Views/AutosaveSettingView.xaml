<UserControl x:Class="WpfApp.Views.AutosaveSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp.Views"
             mc:Ignorable="d">
    <StackPanel>
        <Label Content="{DynamicResource st_autosave_label}" Foreground="White" BorderThickness="0 0 0 1" BorderBrush="#FF7A7A7A"/>
        <CheckBox Content="{DynamicResource st_autosave_enable}" IsChecked="{Binding IsAutosaveEnabled}" Foreground="WhiteSmoke" Margin="0,5,0,0"/>
        <StackPanel Orientation="Horizontal" Margin="0,5">
            <ComboBox ItemsSource="{Binding Timers}" SelectedItem="{Binding SelectedTimer}" Style="{StaticResource DarkComboBox}" Width="200">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding}"/>
                            <TextBlock Text="{DynamicResource st_seconds}"/>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>
    </StackPanel>
</UserControl>
