<UserControl x:Class="WpfApp.Views.LanguageSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Content="Language Settings" Foreground="White" BorderThickness="0 0 0 1" BorderBrush="#FF7A7A7A"/>
        <StackPanel Orientation="Horizontal" Margin="0,5" Grid.Row="1" >
            <ComboBox x:Name="langPanel" Style="{StaticResource DarkComboBox}" Width="200"/>
            <Rectangle Width="5"/>
            <Button Style="{StaticResource GrayButton}" Content="Change language" Width="180" Click="ChangeLang"/>
        </StackPanel>
        
    </Grid>
</UserControl>
